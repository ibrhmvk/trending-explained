{
  "slug": "2026-08-13-crm",
  "title": "How an agent-first CRM writes facts without guessing them",
  "repo": "trycompai/crm",
  "repo_url": "https://github.com/trycompai/crm",
  "stars": 8305,
  "language": "TypeScript",
  "date": "2026-08-13",
  "summary": "Comp AI's open-source CRM makes the autonomous agent the primary user: a Postgres lease queue schedules its work, and a fixed-weight evidence ledger decides whether a fact is written or merely suggested.",
  "explainer_url": "https://scrimba.com/explain/guide0hn47066o",
  "body_html": "<h2>The CRM is where the agent keeps its notes</h2>\n<p>Comp AI's <a href=\"https://github.com/trycompai/crm\">crm</a> hit 8,300 stars in about two weeks by taking a swing at a category everyone claims to be reinventing. Their pitch: most CRMs are a database with a form in front of it, and the \"AI CRMs\" bolt a chat box onto the side of that form. Both still leave the actual work &mdash; finding out what is true about a contact and writing it down &mdash; to a human. This project inverts the relationship. The agent is not a feature of the CRM; the CRM is the agent's notebook. The agent is its own deployment (built on eve, Vercel's filesystem-first framework for durable agents), running on its own schedule against its own work queue, with a research budget it spends and then stops. Close the browser and it keeps going.</p>\n<h2>How it actually works</h2>\n<p>The monorepo is a Turborepo on Bun: a Next.js front end, a NestJS API that talks to the app over tRPC, Prisma on Postgres, and <code>apps/agent</code> as the interesting part. The agent has 18 tools (<code>read_crm_history</code>, <code>identify_contact</code>, <code>record_fact</code>, <code>schedule_recheck</code>&hellip;), four skills that are literally markdown prose versioned like code, and exactly one schedule: a dispatcher that decides nothing. It leases whatever is due from the <code>agentTask</code> table and starts a session per row. The whole work queue is one SQL statement in <code>lib/tasks.ts</code>:</p>\n<pre><code>UPDATE \"agentTask\" AS t\nSET \"leasedUntil\" = ${until},\n    \"attempts\" = t.\"attempts\" + 1\nFROM (\n    SELECT t2.id FROM \"agentTask\" AS t2\n    WHERE t2.\"finishedAt\" IS NULL\n        AND t2.\"dueAt\" &lt;= ${now}\n        AND (t2.\"leasedUntil\" IS NULL OR t2.\"leasedUntil\" &lt; ${now})\n        AND t2.\"attempts\" &lt; ${MAX_ATTEMPTS}\n    ORDER BY t2.\"priority\" DESC, t2.\"dueAt\" ASC\n    LIMIT ${limit}\n    FOR UPDATE SKIP LOCKED\n) AS due\nWHERE t.id = due.id</code></pre>\n<p><code>FOR UPDATE SKIP LOCKED</code> is doing a lot of quiet work here: two dispatchers running concurrently skip each other's locked rows instead of blocking on them, so they take disjoint work with no coordinator. A run that crashes just lets its lease expire and the row becomes claimable again; a row that keeps failing hits <code>MAX_ATTEMPTS</code> and is retired. Anything that smells like \"every N minutes, the ten oldest contacts\" lives in a task's <code>dueAt</code>, not a cron expression &mdash; when the agent wants another look at someone in two weeks, it calls <code>schedule_recheck</code> and must state why, and that reason is shown to the sales rep.</p>\n<h2>Nothing about a person is guessed</h2>\n<p>The detail worth stealing is the evidence ledger. No tool accepts a confidence score, on the theory that a model asked to grade its own certainty will be wrong in the direction that makes it look useful. Instead, tools report what they <em>observed</em>, and <code>lib/evidence.ts</code> prices each kind of observation with a fixed weight:</p>\n<ul>\n<li><code>profile.email-match</code> &mdash; their email address is on the profile: 0.95</li>\n<li><code>crm.thread-reply</code> &mdash; they replied on a thread you have: 0.85</li>\n<li><code>employer-only</code> &mdash; employer matches, name does not: 0.2</li>\n<li><code>contradiction</code> &mdash; another source disagrees: 0</li>\n</ul>\n<p>Arithmetic, not the model, decides the outcome: strong evidence writes to the record, weak evidence becomes a suggestion a human settles. The same paranoia shapes the sandbox. The agent gets a real shell &mdash; <code>bash</code>, <code>grep</code>, <code>glob</code>, a <code>/workspace</code> for keeping dossiers &mdash; but with deny-all egress, which costs nothing because <code>web_fetch</code> runs in the app runtime and <code>web_search</code> at the model provider. And the sandbox is never handed <code>DATABASE_URL</code>. A shell with credentials and network access is exfiltration-shaped even in an internal tool; a shell with neither is a text processor.</p>\n<h2>Who should use it</h2>\n<p>Small teams can genuinely self-host it: Bun, Docker Postgres, one <code>.env</code>, Google or Microsoft sign-in, and it runs with zero external API keys by mining your own threads and signature blocks &mdash; each key (Perplexity, LinkedIn via RapidAPI) just opens one more place to look. It is deliberately single-tenant; the maintainers argue an <code>organizationId</code> that always holds one value is a column, an index and a permissions check that buys nothing. But even if you never run it, it reads well as a reference architecture for durable agents: a Postgres lease queue, budgets, evidence-gated writes, and human-in-the-loop suggestions are patterns that transfer to any agentic system you're building.</p>"
}